name: cya
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management + DI (PRD §4.2). Plain Riverpod (not hooks).
  flutter_riverpod: ^2.6.1

  # Declarative navigation with a stateful bottom-nav shell (PRD §4.2).
  go_router: ^14.6.2
  drift: ^2.34.3
  path_provider: ^2.1.6

  # PBKDF2-HMAC-SHA256 for the device PIN (ADR-010). Pure Dart, offline, and
  # maintained by the Dart team — it adds no network capability, which the
  # local-first constraint (PRD §5.7) does not allow.
  crypto: ^3.0.6

  # Promoted from dev_dependencies: the app now issues `PRAGMA key` on every
  # connection (ADR-010), so the SQLite binding is a runtime dependency rather
  # than just the in-memory engine the tests were using. Which *library* it
  # loads is decided by the build hook — see `hooks:` at the bottom of this
  # file. Note there is deliberately no `sqlcipher_flutter_libs`: sqlite3 3.x
  # dropped `DynamicLibrary` loading in favour of build hooks and tells you to
  # drop that package.
  sqlite3: ^3.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  # Drives the clock in tests that assert time-based behaviour (the midnight
  # rollover). A transitive dep of flutter_test, declared explicitly because
  # we import it directly.
  fake_async: ^1.3.3
  drift_dev: ^2.34.5
  build_runner: ^2.15.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # App assets.
  assets:
    - lib/assets/images/cya-logo.png
    - lib/assets/images/cya-splash-sprite.webp

  # Plus Jakarta Sans, bundled for offline/local-first rendering (PRD §8.1 / §3.5).
  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: lib/assets/fonts/PlusJakartaSans-Regular.ttf
          weight: 400
        - asset: lib/assets/fonts/PlusJakartaSans-Medium.ttf
          weight: 500
        - asset: lib/assets/fonts/PlusJakartaSans-SemiBold.ttf
          weight: 600
        - asset: lib/assets/fonts/PlusJakartaSans-Bold.ttf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Which native SQLite `package:sqlite3` links (ADR-010).
#
# `sqlite3mc` is SQLite3 Multiple Ciphers, configured at runtime to use its
# **SQLCipher 4 scheme** (see `CyaDatabase`). It reads and writes exactly the
# format `net.zetetic:sqlcipher-android` produces on the Kotlin side, which is
# what lets the Share Sheet path and the Flutter UI open the same encrypted
# file.
#
# Why not `source: sqlcipher`, which would be the obvious choice: that build
# hook ships a library called `libsqlcipher.so`, and so does net.zetetic. Only
# one file of a given name survives into the APK, and the one that won was the
# C-only build with no JNI — so every Kotlin open died with
# `UnsatisfiedLinkError`. `libsqlite3mc.so` is a different filename, so both
# libraries ship and each runtime loads the one it can actually call.
hooks:
  user_defines:
    sqlite3:
      source: sqlite3mc
