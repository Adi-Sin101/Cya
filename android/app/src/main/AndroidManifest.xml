<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Reminders are this app's core function (PRD 3.4/5.6), which is what
         qualifies it for exact alarms. The scheduler still degrades to an
         inexact alarm rather than failing if permission is absent. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Alarms do not survive a reboot; they are re-armed from the store. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="Cya!"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <!-- Native, pre-Flutter animated splash. This is the launcher; it plays
             the intro video then hands off to MainActivity (PRD §5.2). -->
        <!-- Native-thin capture (PRD 5.4): an exported, translucent, engine-free
             activity that writes the capture straight to the shared SQLite file
             and dismisses. Nothing may be added to this path. -->
        <activity
            android:name=".capture.CaptureActivity"
            android:exported="true"
            android:theme="@style/CaptureTheme"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:label="Cya!">
            <intent-filter android:label="Save to Cya!">
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter android:label="Save to Cya!">
                <action android:name="android.intent.action.PROCESS_TEXT"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Quick Settings Tile (PRD 6.1): a tile cannot read the screen, so it
             opens a fast native paste/dictate capture. Still no Flutter engine. -->
        <activity
            android:name=".tile.QuickCaptureActivity"
            android:exported="false"
            android:theme="@style/QuickCaptureTheme"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:label="Save to Cya!"/>
        <service
            android:name=".tile.CyaTileService"
            android:exported="true"
            android:icon="@drawable/ic_stat_cya"
            android:label="Save to Cya!"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>
        <activity
            android:name=".SplashActivity"
            android:exported="false"
            android:theme="@style/SplashTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Cya! routes its own deep links: MainActivity hands `cya://promise/<id>`
                 to Dart over the reminder channel, which pushes the promise detail
                 route. Flutter's automatic handling would instead feed the raw URI to
                 go_router as a location, which has no such route. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- cya://promise/<id> — a reminder opens the promise it is about
                 (PRD 3.4: one-tap resolution reachable from the notification). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="cya"/>
            </intent-filter>
        </activity>
        <!-- The resurfacing engine (PRD 5.6). All three are engine-free: a
             reminder can fire, and be resolved, without Flutter ever starting. -->
        <receiver
            android:name=".reminders.ReminderReceiver"
            android:exported="false"/>
        <receiver
            android:name=".reminders.NotificationActionReceiver"
            android:exported="false"/>
        <receiver
            android:name=".reminders.DigestReceiver"
            android:exported="false"/>
        <receiver
            android:name=".reminders.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
